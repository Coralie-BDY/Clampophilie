{% extends 'base.html.twig' %}

{% block title %}Clampophilie - Mon compte{% endblock %}

{% block content %}
    <h1> Mon compte </h1>

    Bienvenue {{ app.user.firstname }} dans votre profil. <br/>
    C'est ici que vous allez pouvoir gérer toutes vos informations personnelles.
    <hr>
    <div class="row">
        <div class="col-md-3">
            <div class="account-item text-center">
                {% if is_granted('ROLE_ADMIN') %}
                    <img src="{{ asset('assets/img/control-panel.png') }}" alt="admin"> <br>
                    <a href="/admin">Accéder administration</a>
                {% endif %}
            </div>
        </div>
        <div class="col-md-3">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/padlock.png') }}" alt="mot de passe"> <br>
                <a href="{{ path('account_password') }}">Modifier mot de passe</a>
            </div>
        </div>
        <div class="col-md-3">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/location-pin.png') }}" alt="adresse"> <br>
                <a href="{{ path('account_address') }}">Modifier adresses</a>
            </div>
        </div>
        <div class="col-md-3">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/trolley.png') }}" alt="commandes"> <br>
                <a href="{{ path('account_order') }}">Voir commandes</a>
            </div>
        </div>
    </div>
    <hr>
    <div class="mb-5"></div>


{% endblock %}
