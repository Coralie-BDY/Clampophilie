{% extends 'base.html.twig' %}

{% block title %}Clampophilie - Ajouter une adresse{% endblock %}

{% block content %}
    <h1> Gérer mon adresse </h1>
    <a href="{{ path('account_add_address') }}">Retour</a> <br/>

    <hr>
    {{ form_start(form) }}
    <div class="row col-md-8 offset-md-2 text-center">
        <div class="col-md-12">{{ form_row(form.name) }}</div>
        <div class="col-md-6">{{ form_row(form.firstname) }}</div>
        <div class="col-md-6">{{ form_row(form.lastname) }}</div>
        <div class="col-md-12">{{ form_row(form.address) }}</div>
        <div class="col-md-6">{{ form_row(form.postalcode) }}</div>
        <div class="col-md-6">{{ form_row(form.city) }}</div>
        <div class="col-md-6 offset-md-3">{{ form_row(form.phone) }}</div>
        <div class="col-md-12">{{ form_row(form.submit) }}</div>
    </div>

    {{ form_end(form) }}
    <div class="mb-5"></div>

{% endblock %}
