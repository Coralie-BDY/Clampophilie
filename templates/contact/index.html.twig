{% extends 'base.html.twig' %}

{% block title %}Clampophilie - Nous contacter{% endblock %}

{% block content %}
    <h2>Nous contacter</h2>
    <div class="row">
        {% for message in app.flashes('notification') %}
            <div class="alert alert-info">{{ message }}</div>
        {% endfor %}

        <div class="col-md-6 offset-md-1 mt-5">
            {{ form(form) }}
        </div>
        <div class="col-md-2 offset-md-2 text-center">
            <strong>CLAMPOPHILIE</strong>
            <hr>
            <p> Tél : 06.00.00.00.00</p>
            <p>Email : contact@clampophilie.fr</p>
            <img src="{{ asset('assets/img/clampophilie.png') }}" alt="Clamp" class="img-fluid">
        </div>
    </div>
    <div class="mb-5"></div>
{% endblock %}
